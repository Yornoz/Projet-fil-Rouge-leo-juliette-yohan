<!DOCTYPE html>
<html>
<head>
  <title>Panneau Admin</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>👑 Panneau Administrateur</h1>

  <a href="/">⬅️ Retour à l’accueil</a>

  <h2>Utilisateurs</h2>
  <ul>
    <% users.forEach(user => { %>
      <li><%= user.name %> - <%= user.email %> (<%= user.role %>)</li>
    <% }) %>
  </ul>

  <h2>Quiz</h2>
  <ul>
    <% quizzes.forEach(quiz => { %>
      <li><%= quiz.title %> — Créé par <%= quiz.author ? quiz.author.name : 'Inconnu' %></li>
    <% }) %>
  </ul>
</body>
</html>
