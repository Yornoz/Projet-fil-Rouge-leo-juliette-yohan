<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Les Quiz - QuIsen</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>

  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow-sm">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">Qu<span class="text-danger">Isen</span></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item"><a class="nav-link" href="/">Accueil</a></li>
          <li class="nav-item"><a class="nav-link" href="/users">Utilisateurs</a></li>
          <li class="nav-item ms-2"><a class="btn btn-start fw-bold" href="/mesquiz">Mes quiz</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- CONTENU PRINCIPAL -->
  <div class="container mt-5 pt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h2>Tous les Quiz</h2>
      <a href="/creerquiz" class="btn btn-success">+ Créer un quiz</a>
    </div>

    <div class="row" id="quizzesList">

      <!-- 🔹 QUIZ STATIQUES PAR DÉFAUT -->
      <div class="col-md-4 mb-4">
        <div class="card shadow-sm h-100">
          <div class="card-body d-flex flex-column">
            <h5 class="card-title">Culture Générale</h5>
            <p class="card-text">Un quiz pour tester ta culture générale !</p>
            <small class="text-muted mb-2">Créé par : Admin</small>
            <div class="mt-auto text-center">
              <a class="btn btn-primary mt-3" href="/quizzes/culture-generale">Jouer</a>
            </div>
          </div>
        </div>
      </div>

      <div class="col-md-4 mb-4">
        <div class="card shadow-sm h-100">
          <div class="card-body d-flex flex-column">
            <h5 class="card-title">Football</h5>
            <p class="card-text">Teste tes connaissances sur le foot ⚽</p>
            <small class="text-muted mb-2">Créé par : Admin</small>
            <div class="mt-auto text-center">
              <a class="btn btn-primary mt-3" href="/quizzes/football">Jouer</a>
            </div>
          </div>
        </div>
      </div>

      <!-- 🔹 QUIZ DE LA BASE DE DONNÉES -->
      <% if (quizzes && quizzes.length) { %>
        <% quizzes.forEach(function(quiz){ %>
          <div class="col-md-4 mb-4">
            <div class="card shadow-sm h-100">
              <div class="card-body d-flex flex-column">
                <h5 class="card-title"><%= quiz.title %></h5>
                <p class="card-text"><%= quiz.description %></p>
                <small class="text-muted mb-2">Créé par : <%= (quiz.author && quiz.author.name) ? quiz.author.name : '—' %></small>
                <div class="mt-auto text-center">
                  <a class="btn btn-primary mt-3" href="/quizzes/<%= quiz._id %>">Jouer</a>
                </div>
              </div>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <p class="text-center text-muted">Aucun quiz enregistré pour le moment.</p>
      <% } %>
    </div>

    <!-- 🔹 Bouton retour à l'accueil -->
    <div class="text-center mt-5">
      <a href="/" class="btn btn-outline-secondary">🏠 Retour à l’accueil</a>
    </div>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
